            var post = {
            id: data.id,
            url: data.url,
            domain: data.domain,
            comment_count: data.num_comments,
            permalink: data.permalink,
            title: data.title,
            selftext: false,
            thumbnail: false
        };

            _.each(response.data['Time Series (60min)'], function(item) {
                var data = item.data;
                console.log(data);
            }